#include<iostream>
using namespace std;
void PreOrder(TreeNode*head)
{
    if(head == NULL)
    {
        return;
    }
    cout<<head->data;
    preorder(head->left);
    preorder(head->right);
}
void Inorder(TreeNode* head)
{
    if(head == NULL)
    {
        return;
    }
    Inorder(head->left);
    cout<<head->data;
    Inorder(head->right);
}
void Postorder(TreeNode* head)
{
    if(head == NULL)
    {
        return;
    }
    Inorder(head->left);
    Inorder(head->right);
    cout<<head->data;
}
int main(){



    return 0;
}
